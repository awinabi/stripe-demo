
<%=form_with url:charges_path, local:true do |f|%>
  <%= f.submit 'submit', id:"my_id", onclick: "javascript: callMethod('#{@checkout_session.id}')" %>
<%end%>


<%= button_tag 'Checkout', id: 'my_id', onclick: "callMethod('#{@checkout_session.id}', event);" %>



